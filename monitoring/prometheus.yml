global:
  scrape_interval: 5s  # Collecte les métriques toutes les 5 secondes

scrape_configs:
  - job_name: 'backend'
    static_configs:
      - targets: ['backend:3000']  # "backend" = nom du service dans docker-compose